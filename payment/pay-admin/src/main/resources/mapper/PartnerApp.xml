<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yijia360.pay.admin.dao.partner.PartnerAppDao">

	
	<update id="update" parameterType="PartnerApp">
	        UPDATE pay_partner_app
	        <trim prefix="SET" suffixOverrides=",">
		        <if test="appName != null and appName !=''">
		        	appName = #{appName},
		        </if>
		        <if test="notify != null and notify != ''">
		        	notify = #{ notify },
		        </if>
		        <if test="appSecret != null and appSecret != ''">
		        	appSecret = #{appSecret},
		        </if>
		        <if test="salt != null and salt != ''">
		        	salt = #{salt},
		        </if>
		        <if test="isIpLimit != null">
		        	isIpLimit = #{isIpLimit},
		        </if>
		        <if test="ip != null and ip != ''">
		        	ip = #{ ip },
		        </if>
		        <if test="state != null">
		        	state = #{state},
		        </if>
	        </trim>
	        WHERE partnerId=#{partnerId} and appId = #{appId}
	</update>
	
	<insert id="save"  parameterType="PartnerApp">
	 	<![CDATA[
	        INSERT INTO pay_partner_app (
	        	partnerId, appName, appId, salt,appSecret,notify,isIpLimit,ip
	        ) VALUES (
	        
	        #{partnerId},#{appName},#{appId},#{salt},#{appSecret},#{notify},#{isIpLimit},#{ip})
	    ]]> 
	</insert>
	
	<select id="get" parameterType="PartnerApp" resultType="PartnerAppVo">
		SELECT * FROM pay_partner_app
		<where>
			<if test="appId != null">
				And appId = #{appId}
			</if>
			<if test="appName !='' and appName !=null">
				AND appName like CONCAT(#{appName} ,'%')
			</if>
		</where>
	</select>
	
	<select id="count" parameterType="PartnerApp" resultType="java.lang.Integer">
		SELECT count(partnerId) FROM pay_partner_app
		<where>
			<if test="appName !='' and appName !=null">
				AND appName like CONCAT(#{appName} ,'%')
			</if>
		</where>
	</select>
	<select id="list" parameterType="PartnerApp" resultType="PartnerAppVo">
		SELECT * FROM pay_partner_app
		<where>
			<if test="appName !='' and appName !=null">
				AND appName like CONCAT(#{appName} ,'%')
			</if>
		</where>
			LIMIT #{ start }, #{ pageSize }
	</select>
	<select id="getList" parameterType="PartnerApp" resultType="PartnerAppVo">
		SELECT * FROM pay_partner_app
		<where>
			<if test="partnerId !=null">
				AND partnerId = #{partnerId}
			</if>
		</where>
	</select>
</mapper>