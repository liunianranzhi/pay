<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yijia360.pay.admin.dao.partner.PartnerDao">

	
	<update id="update" parameterType="Partner">
	        UPDATE pay_partner
	        <trim prefix="SET" suffixOverrides=",">
		        <if test="partnerName != null and partnerName !=''">
		        	partnerName = #{partnerName},
		        </if>
		        <if test="partnerMail != null and partnerMail !=''">
		        	partnerMail = #{partnerMail},
		        </if>
		        <if test="operator != null and operator !=''">
		        	operator = #{operator},
		        </if>
		        <if test="mobile != null and mobile != ''">
		        	mobile = #{ mobile },
		        </if>
		        <if test="state != null">
		        	state = #{state},
		        </if>
	        </trim>
	        WHERE partnerId=#{partnerId}
	</update>
	
	<insert id="save"  parameterType="Partner">
	 	<![CDATA[
	        INSERT INTO pay_partner (
	        	partnerName, partnerMail, operator, mobile,state
	        ) VALUES (
	        
	        #{partnerName},#{partnerMail},#{operator},#{mobile},#{state})
	    ]]> 
	</insert>
	
	<select id="get" parameterType="Partner" resultType="PartnerVo">
		SELECT * FROM pay_partner
		<where>
			<if test="partnerId != null">
				And partnerId = #{partnerId}
			</if>
			<if test="partnerName !='' and partnerName !=null">
				AND partnerName like CONCAT(#{partnerName} ,'%')
			</if>
			<if test="mobile != null and mobile != ''">
				AND mobile = #{mobile}
			</if>
		</where>
	</select>
	
	<select id="count" parameterType="Partner" resultType="java.lang.Integer">
		SELECT count(partnerId) FROM pay_partner
		<where>
			<if test="partnerId != null">
				And partnerId = #{partnerId}
			</if>
			<if test="partnerName !='' and partnerName !=null">
				AND partnerName like CONCAT(#{partnerName} ,'%')
			</if>
			<if test="mobile != null and mobile != ''">
				AND mobile = #{mobile}
			</if>
		</where>
	</select>
	<select id="list" parameterType="Partner" resultType="PartnerVo">
		SELECT * FROM pay_partner
		<where>
			<if test="partnerId != null">
				And partnerId = #{partnerId}
			</if>
			<if test="partnerName !='' and partnerName !=null">
				AND partnerName like CONCAT(#{partnerName} ,'%')
			</if>
			<if test="mobile != null and mobile != ''">
				AND mobile = #{mobile}
			</if>
		</where>
			LIMIT #{ start }, #{ pageSize }
	</select>
	<select id="getList" parameterType="Partner" resultType="PartnerVo">
		SELECT * FROM pay_partner
		<where>
			<if test="partnerId != null">
				And partnerId = #{partnerId}
			</if>
		</where>
	</select>
	
	<insert id="saveAndGetId" useGeneratedKeys="true" keyProperty="partnerId" parameterType="Partner">  
	    <![CDATA[
	        INSERT INTO pay_partner (
	        	partnerName, partnerMail, operator, mobile,state
	        ) VALUES (
	        #{partnerName},#{partnerMail},#{operator},#{mobile},#{state})
		    ]]>
	</insert>  
</mapper>